# 🦁 멋쟁이사자처럼 - 학습 집중도 분석 시스템 (CV Focus Project)

웹캠 이미지를 실시간으로 분석하여 학생들의 수업 집중도를 파악하고, 관리자가 이를 모니터링할 수 있는 AI 기반 웹 서비스입니다.

---

## ⚙️ 로컬 환경 실행 방법 (How to Run)

이 프로젝트는 **프론트엔드(Node.js)**와 **AI 백엔드(FastAPI)** 두 개의 서버를 동시에 켜야 완벽하게 작동합니다. 터미널 창을 2개 띄워주세요!

### 1️⃣ 프론트엔드 서버 실행 (웹 화면)
프론트엔드 폴더(`CV_Focus_Project` 최상위)에서 터미널을 열고 아래 명령어를 실행합니다.

패키지 설치 및 로컬 서버 실행 (최초 1회만)
```bash
cd frontend
npm install
node index.js
```

👉 실행 후 브라우저에서 `http://localhost:3000` 으로 접속합니다.

### 2️⃣ AI 백엔드 서버 실행 (FastAPI)
CNN 모델과 룰베이스 로직이 돌아가는 파이썬 서버입니다. 백엔드 폴더(`main.py`가 있는 곳)에서 터미널을 열고 아래 명령어를 실행합니다.

필요한 파이썬 라이브러리 설치 및 FastAPI 서버 실행(다른 터미널에서 최초 1회만)
```bash
cd ai_server
pip install -r requirements.txt
uvicorn main:app --host 127.0.0.1 --port 8000 --reload
```

👉 `✅ [AI 서버] 모델 로드 완벽 성공!` 메시지가 뜨면 프론트엔드와 통신할 준비가 완료된 것입니다.

---

## 🎯 핵심 파이프라인
* **Student:** 웹캠 접속 → MediaPipe 랜드마크 추출 → 눈/입 크롭 이미지 FastAPI 서버로 전송
* **AI Server (FastAPI):** CNN 모델 분석 → 집중도 확률 프론트엔드로 반환
* **Firebase DB:** 0.5초 단위 점수 결산 → 수업 종료 시 일간 평균 및 타임라인 데이터 누적 저장

* **Admin:** 실시간 전체 학생 모니터링 및 월별/일별 상세 그래프 조회

---

## 🧠 서비스 흐름 (Service Flow)

1. **수업 시작**  
   학생과 관리자가 웹페이지에 접속하고, 관리자의 시작 신호로 실시간 모니터링이 활성화됩니다.

2. **실시간 특징 추출**  
   웹캠 영상을 기반으로 MediaPipe를 활용하여 얼굴 랜드마크, 시선, 눈 깜빡임, 상체 자세 등의 특징을 추출합니다.

3. **집중도 분석**  
   Rule-based 로직과 CNN 모델을 결합하여 집중 상태와 점수를 산출하며, 졸음·이탈 발생 시 관리자에게 즉시 전달됩니다.

4. **데이터 저장**  
   분석 결과는 Firebase에 실시간 저장되며, 상태 변화 기반으로 DB write를 최적화합니다.

5. **수업 종료 및 리포트 생성**  
   수업 종료 시 누적 데이터를 기반으로 개인별 리포트와 수업 타임라인이 자동 생성됩니다.

6. **관리자 기능**  
   관리자 페이지에서 실시간 모니터링, 월별 분석, 타임라인 확인, PDF 출력 및 과정/학생 관리가 가능합니다.

---

## 🏗️ 시스템 아키텍처 (Architecture)

<img width="1024" height="559" alt="image" src="https://github.com/user-attachments/assets/6df57672-4862-4843-8eec-ae453878cf84" />


- **Frontend (HTML, CSS, JavaScript)**  
  웹캠 영상 캡처 및 관리자 대시보드 UI 제공

- **Backend (FastAPI)**  
  프레임 수신, 세션 관리, AI 모델 연동 및 분석 결과 반환

- **AI / Model (MediaPipe + MobileNet CNN)**  
  얼굴/시선/자세 분석 및 졸음 탐지 후 집중도 계산

- **Database (Firebase Firestore)**  
  실시간 상태, 타임라인, 리포트 데이터 저장 및 관리

---

## 🚀 주요 특징 (Key Features)

- 실시간 집중도 분석 (웹캠 기반)
- Rule-based + CNN Hybrid 구조
- 상태 머신 기반 집중도 평가
- Firebase 기반 실시간 데이터 동기화
- 관리자 대시보드 및 리포트 자동 생성

---

## 🔥 한 줄 요약

> 웹캠 기반 행동 데이터를 실시간으로 분석하여 집중도를 산출하고, 이를 관리자 모니터링 및 리포트 시스템으로 확장한 End-to-End AI 서비스
